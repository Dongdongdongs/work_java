package test02;

public class Exec {

		public static void main(String[] args) {
			
			Son s = new Son();
			
			
			// son 은 son, father, object 으로 불려질수 있음
			
			// 접근제어자 public 다 쓸수 있음, private 자기 클래스에서만 사용, default 자기 패키지 안에서만, protected 상속을 받았다는 전제하에 다른 패키지에서 사용 가능 
			
			// 크기순
			// public			
			// protected
			// default
			// private
		}
}
